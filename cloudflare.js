// Arquivo para garantir que o Cloudflare Pages reconheça o projeto como compatível
export default {
  platform: "cloudflare",
  config: {
    // Configurações específicas do Cloudflare
    env: {
      // Variáveis de ambiente que serão usadas no deploy
    }
  }
}
